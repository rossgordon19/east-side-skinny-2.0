import React from 'react';

const Listen = () => {
  return (
    <div name='listen' className="bg-black text-white h-[80vh] w-full flex flex-col justify-center items-center">
      <h1 className="text-5xl text-center">Listen</h1>
      <h1 className="mt-4">Streaming Links</h1>
    </div>
  );
};

export default Listen;
